import styled from "styled-components";

export const SubTitle = styled.h2`

font-family: 'DM Sans', sans-serif;
font-size: 42px;
letter-spacing: 1.5px;
color: ${(prop) => prop.cor ? prop.cor : '#000000'};

    `;